(jbuild_version 1)

(executable (
    (name test)
    (libraries (lt4la ppx_expect.evaluator ppx_inline_test.runner.lib))
    (preprocess (pps (ppx_jane ppx_driver.runner (-inline-test-lib test))))
))

(alias (
    (name runtest)
    (deps (test.exe))
    (action (chdir ${ROOT} (run ${<} inline-test-runner lt4la)))
))

(alias (
    (name runtest)
    (deps (test.exe))
    (action (chdir ${ROOT} (run ${<} inline-test-runner test)))
))
