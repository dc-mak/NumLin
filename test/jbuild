(jbuild_version 1)

(library (
    (name test)
    (flags (:standard -short-paths))
    (libraries (lt4la))
    (preprocess (pps (ppx_jane)))
    (inline_tests)
))

(rule (
    (targets (examples.ml))
    (deps (examples/generate.exe (glob_files examples/*.lt)))
    (action (run ${<}))
))
