(jbuild_version 1)

(executables (
    (names (repl transpile))
    (modules (eval repl transpile))
    (libraries (lt4la core core_bench lwt lambda-term))
    (flags (:standard -short-paths))
    (preprocess (pps (ppx_jane lwt_ppx ppx_driver.runner)))
))

(executable (
    (name benchmark)
    (modules (benchmark))
    (libraries (owl measure_kalman_c_ffi test core core_bench))
    (flags (:standard -short-paths))
    (preprocess (pps (ppx_jane)))
))

(library (
    (name bindings)
    (modules (bindings))
    (libraries (ctypes.stubs ctypes))
))
