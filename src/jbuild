(jbuild_version 1)

(library (
    (name lt4la)
    (synopsis "Linear Types for Linear Algebra")
    (flags (:standard -short-paths))
    (libraries (
        owl
        menhirLib
        base
        ppx_expect.evaluator
        ppx_inline_test.runner.lib
    ))
    ; (inline_tests) when jbuilder has pushed support for it
    (preprocess (pps (ppx_jane ppx_driver.runner (-inline-test-lib lt4la))))
))

(menhir (
    (modules (parser))
    ; add --infer when jbuilder has pushed support for it
    (flags (--table --strict --comment --explain))
))

(ocamllex (lexer))
